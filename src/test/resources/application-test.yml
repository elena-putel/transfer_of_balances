spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/transfer_test
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

informix:
  datasource:
    enabled: false

transfer:
  scheduled:
    enabled: false

logging:
  level:
    com.beltelecom.transfer: DEBUG
